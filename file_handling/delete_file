import os

pth = os.path.join('my_first_file.txt')
try:
  os.remove(pth)
except FileNotFoundError:
  print('File is already deleted')

if os.path.exists(pth):
  os.remove(pth)
else:
  print('file already deleted')
